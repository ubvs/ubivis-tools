# Infisical Secrets Manager - DevContainer Configuration

# Keys
# Required key for platform encryption/decryption ops
ENCRYPTION_KEY=6c1fe4e407b8911c104518103505b218

# JWT
# Required secrets to sign JWT tokens
AUTH_SECRET=5lrMXKKWCVocS/uerPsl7V+TX/aaUaI7iDkgl3tSmLE=

# Postgres Configuration
POSTGRES_USER=infisical
POSTGRES_PASSWORD=infisical
POSTGRES_DB=infisical

# Database Connection
DB_CONNECTION_URI=postgres://infisical:infisical@db:5432/infisical

# Redis Configuration
REDIS_URL=redis://redis:6379

# Website URL
SITE_URL=http://localhost:8081

# Telemetry - ALL DISABLED (no data sent to Infisical)
TELEMETRY_ENABLED=false

# Disable all external tracking and monitoring
SENTRY_DSN=
POSTHOG_HOST=
POSTHOG_PROJECT_API_KEY=
OTEL_TELEMETRY_COLLECTION_ENABLED=false

# Mail/SMTP (Optional - configure if needed)
SMTP_HOST=
SMTP_PORT=
SMTP_FROM_ADDRESS=
SMTP_FROM_NAME=
SMTP_USERNAME=
SMTP_PASSWORD=

# SSO Configuration (Optional - FREE for GitHub/Google)
# See SSO_INTEGRATION.md for setup instructions

# GitHub SSO (Free)
CLIENT_ID_GITHUB_LOGIN=
CLIENT_SECRET_GITHUB_LOGIN=

# Google SSO (Free)
CLIENT_ID_GOOGLE_LOGIN=
CLIENT_SECRET_GOOGLE_LOGIN=

# GitLab SSO (Free)
CLIENT_ID_GITLAB_LOGIN=
CLIENT_SECRET_GITLAB_LOGIN=

# Keycloak OIDC (Requires Enterprise License - OR use patch-oidc.sh to enable)
# After patching, configure these:
OIDC_DISCOVERY_URL=http://ssom-keycloak:8080/realms/ubivis/.well-known/openid-configuration
OIDC_CLIENT_ID=secrets
OIDC_CLIENT_SECRET=
# OIDC_SIGNATURE_ALGORITHM=RS256


# Node Environment
NODE_ENV=production
